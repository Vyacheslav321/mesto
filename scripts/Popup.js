class Popup {
  constructor(popupSelector) {}
  open() {  //открытие попапа

  };
  close() { //закрытие попапа
    
  };
  _handleEscClose() { //закрытие попапа по esc

  };
  setEventListeners() { //слушатель клика по крестику и серой области

  };
}
